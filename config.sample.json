[
  {
    "parentSelector": ".info-tile",
    "childSelectors": [
      { "selector": ".sensitive-data", "method": "blur", "level": "8px" },
      { "selector": ".confidential", "method": "scramble" }
    ]
  },
  {
    "parentSelector": ".profile-card",
    "childSelectors": [
      { "selector": ".private-info", "method": "blur" } // Default level will be used
    ]
  }
]
